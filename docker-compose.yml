version: '3.8'

services:
  db:
    image: mysql:8.0
    container_name: toko_ht_mysql
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: toko_ht
      MYSQL_USER: user
      MYSQL_PASSWORD: password
    ports:
      - "3307:3306"
    volumes:
      - db_data:/var/lib/mysql

volumes:
  db_data:
